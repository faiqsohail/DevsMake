import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#6f339a',
    },
    secondary: {
      main: '#9b64ba',
    },
  },
});

export default theme;
